# API base URI — override via -Dapi.base.uri=... or env API_BASE_URI
# api.base.uri=https://api.example.com

# Preferred test-data setup:
# - Use src/test/resources/testdata.properties for market/order/deposit data.
# - Use api.base.uri.public and api.base.uri.internal there for separate base URLs.

# API version prefix if needed (e.g. /v1)
# api.version=v1
